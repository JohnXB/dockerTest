<p>
  <strong>标题:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>内容:</strong>
  <%= @article.text %>
</p>
<h2>评论列表</h2>
<%= render @article.comments %>
<h2>添加评论:</h2>
<%= render 'comments/form'  %>
<%= link_to '编辑', edit_article_path(@article) %> |
<%= link_to '返回首页', articles_path %>|
<%= link_to '返回新建页', new_article_path %>
